describe("workout", function () {

    beforeEach(function () {

    });

    xit("scenario should create a scene", function () {
        ux.runner.run();
        expect(ux.runner.dispatcher.events.length).toBe(17);
    });
});